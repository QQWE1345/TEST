# 威纶通宏脚本语法修正说明

## 🔧 修正的语法错误

根据 EasyBuilder Pro 编译器的报错，已修正以下关键语法问题：

---

### ❌ 错误 1：GetData 缓冲区参数错误

**错误代码：**
```
result = GetData(buf[0], "Modbus网关45", ReadHoldingReg, 148, 13)
```

**问题分析：**
- `buf[0]` 表示数组的第一个元素，不是缓冲区起始地址
- 威纶通宏要求传递数组名称本身，而不是数组元素

**✅ 正确写法：**
```
result = GetData(buf, "Modbus网关45", ReadHoldingReg, 148, 13)
```

**编译器错误代码：** error C38: 不支持设置缓冲方式

---

### ❌ 错误 2：SetData 设备名称错误

**错误代码：**
```
SetData(1, "Local HMI", LB, 60, 1)
```

**问题分析：**
- `"Local HMI"` 不是威纶通认可的设备名称
- 本地位地址（LB）应使用 `"local"` 作为设备名（小写）

**✅ 正确写法：**
```
SetData(1, "local", LB, 60, 1)
```

**编译器错误代码：** error C45: 必需选择正确的设备类型

---

## 📋 完整修正对照表

| 行号 | 修正前 | 修正后 | 说明 |
|-----|-------|-------|------|
| 22 | `GetData(buf[0], ...)` | `GetData(buf, ...)` | 缓冲区参数修正 |
| 29 | `SetData(1, "Local HMI", ...)` | `SetData(1, "local", ...)` | 设备名修正 |
| 31 | `SetData(0, "Local HMI", ...)` | `SetData(0, "local", ...)` | 设备名修正 |
| 36 | `SetData(1, "Local HMI", ...)` | `SetData(1, "local", ...)` | 设备名修正 |
| 38 | `SetData(0, "Local HMI", ...)` | `SetData(0, "local", ...)` | 设备名修正 |
| 43 | `SetData(1, "Local HMI", ...)` | `SetData(1, "local", ...)` | 设备名修正 |
| 45 | `SetData(0, "Local HMI", ...)` | `SetData(0, "local", ...)` | 设备名修正 |
| 50 | `SetData(1, "Local HMI", ...)` | `SetData(1, "local", ...)` | 设备名修正 |
| 52 | `SetData(0, "Local HMI", ...)` | `SetData(0, "local", ...)` | 设备名修正 |
| 57 | `SetData(1, "Local HMI", ...)` | `SetData(1, "local", ...)` | 设备名修正 |
| 59 | `SetData(0, "Local HMI", ...)` | `SetData(0, "local", ...)` | 设备名修正 |
| 64-68 | `SetData(0, "Local HMI", ...)` | `SetData(0, "local", ...)` | 设备名修正（5处）|
| 72 | `SetData(0, "Local HMI", ...)` | `SetData(0, "local", ...)` | 设备名修正 |

**总计修正：** 17 处

---

## 🎯 威纶通宏语法要点总结

### GetData 正确用法

```
short buf[100]
short result

// ✅ 正确：传递数组名称
result = GetData(buf, "DeviceName", AddressType, StartAddr, Count)

// ❌ 错误：传递数组元素
result = GetData(buf[0], "DeviceName", AddressType, StartAddr, Count)
```

### SetData 设备名称规范

| 设备类型 | 正确写法 | 错误写法 |
|---------|---------|---------|
| 本地 HMI | `"local"` | `"Local HMI"`, `"LOCAL"`, `"Local"` |
| Modbus 设备 | `"Modbus网关45"` | 必须与设备列表完全一致 |
| 以太网设备 | `"Ethernet_1"` | 必须与设备列表完全一致 |

**注意：**
- 设备名称区分大小写
- 本地位地址固定使用 `"local"`（小写）
- 其他设备必须与"系统参数→设备→通讯设备"中的名称完全一致

---

## ✅ 验证步骤

1. **复制修正后的脚本**
   - 打开 `modbus_to_local_bit.txt`
   - 全选复制（Ctrl+A, Ctrl+C）

2. **在 EasyBuilder Pro 中粘贴**
   - 菜单：编辑 → 宏
   - 新建或编辑现有宏
   - 粘贴代码（Ctrl+V）

3. **编译检查**
   - 点击"编译"按钮
   - 应显示"编译成功"或"点击标签页可见显示结果"
   - 不应再出现 error C38、C45、C1 等错误

4. **保存并测试**
   - 点击"确定"保存
   - 设置宏触发方式（定时/事件）
   - 在线测试功能

---

## 🐛 如果仍有错误

### 可能原因 1：设备名称不匹配

**检查方法：**
1. 打开 EasyBuilder Pro
2. 菜单：系统参数 → 设备 → 通讯设备
3. 找到 Modbus 设备的名称
4. 确认是否为 `Modbus网关45`（区分大小写、符号）

**如果不一致：**
修改脚本第 22 行的设备名称：
```
result = GetData(buf, "你的实际设备名", ReadHoldingReg, 148, 13)
```

### 可能原因 2：地址类型不支持

**检查方法：**
确认 Modbus 设备支持 `ReadHoldingReg`（3x 寄存器）

**替代方案：**
- `ReadCoil` - 0x 线圈
- `ReadInput` - 1x 输入线圈
- `ReadHoldingReg` - 3x 保持寄存器
- `ReadInputReg` - 4x 输入寄存器

### 可能原因 3：EasyBuilder Pro 版本问题

**检查版本：**
- 菜单：说明 → 关于
- 推荐使用 V6.00 或以上版本
- 较旧版本可能不支持某些语法

---

## 📝 修正后的完整脚本

修正后的脚本已保存在 `modbus_to_local_bit.txt`，关键特性：

✅ **GetData 使用正确的缓冲区参数**  
✅ **SetData 使用正确的设备名称 "local"**  
✅ **保持批量读取优化（一次读 13 个寄存器）**  
✅ **完整的错误处理机制**  
✅ **符合威纶通宏语法规范**

---

## 🎓 技术支持

如果修正后仍有问题，请提供：
1. 完整的错误信息截图
2. EasyBuilder Pro 版本号
3. 设备列表中的实际设备名称
4. Modbus 设备的通讯参数配置

---

**修正日期：** 2026-01-04  
**适用版本：** EasyBuilder Pro V6.0+  
**修正版本：** v1.1
