# ⚠️ 威纶通宏脚本 v1.2 更新说明

## 🔴 发现新的关键错误：for 循环语法错误

### error C21: 非法表达式 (没有'next')

---

## 🆕 v1.2 新增修正

### ❌ 错误 3：for 循环结束语句错误

**错误代码（第 14-16 行）：**
```
for i = 0 to 12
  buf[i] = 0
end for          ← 错误！威纶通不支持 end for
```

**编译器错误：**
- error C21: 非法表达式 (没有'next')

**✅ 正确写法：**
```
for i = 0 to 12
  buf[i] = 0
next i           ← 正确！必须使用 next + 循环变量名
```

---

## 📋 完整修正清单（v1.0 → v1.2）

| 版本 | 修正内容 | 行号 | 错误代码 |
|------|---------|------|----------|
| v1.1 | GetData 缓冲区参数 | 22 | error C38 |
| v1.1 | SetData 设备名称 | 29, 31, 36, 38... | error C45 |
| v1.2 | for 循环结束语句 | 16 | error C21 |

**总修正：** 18 处

---

## 🎯 威纶通宏 for 循环语法规则

### 正确语法格式：

```
for 循环变量 = 起始值 to 结束值
  // 循环体
next 循环变量
```

### 语法对比：

| 语言 | for 循环结束语法 | 威纶通宏是否支持 |
|------|----------------|-----------------|
| BASIC | `next i` | ✅ 支持（推荐）|
| VB | `Next i` | ✅ 支持 |
| Pascal | `end` | ❌ 不支持 |
| Python | 无需结束语句 | ❌ 不支持 |
| 威纶通旧语法 | `end for` | ❌ 不支持 |

**结论：** 威纶通宏使用类似 BASIC 的语法，必须用 `next 变量名`

---

## 📖 完整的 for 循环示例

### 示例 1：初始化数组
```
short arr[10]
short i

for i = 0 to 9
  arr[i] = 0
next i
```

### 示例 2：累加
```
short sum
short i

sum = 0
for i = 1 to 10
  sum = sum + i
next i
// sum = 55
```

### 示例 3：步进（如果支持）
```
for i = 0 to 10 step 2
  // 处理 i = 0, 2, 4, 6, 8, 10
next i
```

---

## ⚡ 请立即更新！

### 方式 A：使用最新主文件
```
1. 打开：modbus_to_local_bit.txt
2. 全选复制（Ctrl+A, Ctrl+C）
3. 在 EBPro 中完全清空旧代码
4. 粘贴新代码
5. 编译验证
```

### 方式 B：使用专用最终版文件
```
1. 打开：最终修正版-请使用此文件.txt
2. 按文件中的说明操作
3. 复制指定区域的代码
4. 粘贴到 EBPro
5. 编译验证
```

---

## ✅ 验证清单

编译成功后，应该看到：
- ✅ 0 error(s) 0 warning(s)
- ✅ 或者显示"编译成功"

不应再有以下错误：
- ❌ error C38: 不支持设置缓冲方式
- ❌ error C45: 必需选择正确的设备类型
- ❌ error C21: 非法表达式 (没有'next')
- ❌ error C31: 参数类型不正确
- ❌ error C1: 语法错误

---

## 🔧 如果还有 error C31（参数类型不正确）

这通常是因为前面的错误导致的连锁反应。修正上述 3 个主要错误后，C31 应该会自动消失。

如果仍然存在，可能原因：
1. 设备名称 "Modbus网关45" 与实际配置不符
2. LB 地址超出范围（检查 HMI 支持的 LB 范围）
3. 数据类型不匹配（应该不会，因为都是 short）

---

## 📞 关键要点总结

### 威纶通宏语言的 3 大语法特点：

1. **GetData 缓冲区**
   - ✅ `GetData(buf, ...)`
   - ❌ `GetData(buf[0], ...)`

2. **SetData 本地位设备名**
   - ✅ `SetData(1, "local", LB, ...)`
   - ❌ `SetData(1, "Local HMI", LB, ...)`

3. **for 循环结束**
   - ✅ `next i`
   - ❌ `end for`

---

## 📁 推荐文件

| 文件名 | 用途 | 推荐度 |
|-------|------|--------|
| `modbus_to_local_bit.txt` | 主脚本文件（已更新 v1.2）| ⭐⭐⭐⭐⭐ |
| `最终修正版-请使用此文件.txt` | 带详细说明的完整脚本 | ⭐⭐⭐⭐⭐ |
| `重要-v1.2更新说明.md` | 本文件，说明新修正 | ⭐⭐⭐⭐ |
| `语法修正说明.md` | v1.1 的修正说明 | ⭐⭐⭐ |
| `README_宏脚本说明.md` | 完整技术文档 | ⭐⭐⭐⭐ |

---

**更新日期：** 2026-01-04  
**当前版本：** v1.2  
**状态：** 所有已知语法错误已修正  
**编译状态：** 应该可以通过 ✅

---

## 🚀 下一步

1. **立即更新代码**：使用 `最终修正版-请使用此文件.txt` 中的代码
2. **重新编译**：确保 0 errors
3. **测试功能**：上传到 HMI 验证功能
4. **监控运行**：在画面上显示 LB60-LB65 状态

祝您使用顺利！🎉
